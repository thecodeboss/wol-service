{
  "name": "wol-service",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend run build",
    "generate-env": "npm run --prefix server generate-env",
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix frontend run dev\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "workspaces": [
    "frontend",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
